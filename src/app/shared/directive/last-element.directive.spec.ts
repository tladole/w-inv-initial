import { LastElementDirective } from './last-element.directive';

describe('LastElementDirective', () => {
  it('should create an instance', () => {
    const directive = new LastElementDirective();
    expect(directive).toBeTruthy();
  });
});
